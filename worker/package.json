{
  "name": "yutian-doll-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260907.1",
    "@cloudflare/vitest-plugin": "^1.0.0",
    "typescript": "~5.9.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.129.0"
  }
}
